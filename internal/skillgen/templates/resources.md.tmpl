# Plane CLI Resource Reference

Auto-generated on {{ .Generated }}.
{{ range $topic := .Topics }}
## {{ $topic.DisplayName }}
{{ range $topic.Actions }}
### `plane {{ $topic.Name }} {{ .CLIName }}`

MCP tool: `{{ .MCPName }}`
{{ if .HasSpec }}
**{{ .Method }}** `{{ .Path }}`
{{ if .Params }}
| Flag | Type | Required | Notes |
|------|------|----------|-------|
{{- range .Params }}
| `--{{ .FlagName }}` | {{ .Type }} | {{ if .Required }}yes{{ else }}no{{ end }} | {{ .Notes }} |
{{- end }}
{{ end }}
{{- else }}
*No cached spec available. Run `plane docs update-specs` to populate.*
{{ end }}
{{- end }}
{{- if $topic.HasEnsure }}

### `plane {{ $topic.Name }} ensure`

Idempotent create-or-update by name. Accepts the same flags as create/update.
{{- end }}
{{ end }}
